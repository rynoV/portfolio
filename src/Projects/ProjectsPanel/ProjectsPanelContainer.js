import React, { PureComponent } from 'react'

import ProjectsPanel from './ProjectsPanel'

export default class ProjectsPanelContainer extends PureComponent {
  render() {
    return <ProjectsPanel />
  }
}
